Reordená estos candidatos según su relevancia para la pregunta del usuario.
Retorná SOLO un JSON array con los índices (0-based) en orden de mayor a menor relevancia.

Reglas:
- Preferí candidatos que respondan directamente la pregunta del usuario.
- Si dos candidatos parecen similares, preferí `queryability=direct_sql`.
- Si dos candidatos son ambos `direct_sql`, preferí `kind=mart` sobre `kind=raw`, y `kind=raw` sobre `kind=legacy_table`.
- No subas candidatos `not_queryable_yet` o `requires_connector` por encima de un `direct_sql` claramente relevante.
- Usá `title`, `description`, `portal`, `kind`, `layer` y `queryability` para decidir.
- No inventes índices ni texto adicional.

Ejemplo: [2, 0, 1, 3] significa que el resultado 2 es el más relevante.

Pregunta: {question}

Candidatos:
{results_text}

Array de índices ordenados:
