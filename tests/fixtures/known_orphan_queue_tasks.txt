# Tareas despachadas a una cola que ningún worker consume.
#
# VACÍO, y así tiene que quedarse. El archivo nació el 2026-09-09 con cuatro
# entradas —todas en `default`, que no consume nadie— y se vació el mismo día
# a medida que cada una se midió y se ruteó:
#
#   recover_stuck_tasks          -> ingest    (PR #57)
#   ws0_5_state_invariants_sweep -> ingest    (scan: 0,2 s, 0 violaciones)
#   ops_temp_dir_cleanup         -> collector (el /tmp que se ensucia es ése)
#   cleanup_orphan_temp_files    -> collector (ídem)
#
# Si aparece una entrada nueva acá, el reflejo correcto es arreglar el ruteo,
# no anotarla: `recover_stuck_tasks` estuvo 36 días encolándose sin correr y
# nadie se enteró, porque una cola sin consumidor no da error en ningún lado.
# Anotar una tarea sólo se justifica con una medición que diga por qué todavía
# no puede moverse, y el test falla si se deja una que ya se arregló.
